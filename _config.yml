# Site settings
title: Jekyll test site
description: > # this means to ignore newlines until "baseurl:"
  A site currently under construction.
baseurl: "" # the subpath of the site, e.g. /blog/
url: "http://clauswilke.github.io" # the base hostname & protocol

# Build settings
markdown: kramdown

# kramdown settings
kramdown:
  input: GFM
